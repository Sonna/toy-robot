#!/usr/bin/env ruby

require "bundler/setup"
require "toy_robot"

filename = ARGV.first

input = filename ? ToyRobot::FileInput.new(filename) : ToyRobot::PlayerInput.new

robot = ToyRobot::Robot.new(input)

loop { break if input.update == "EXIT" }
